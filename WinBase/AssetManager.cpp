#include "AssetManager.h"

shared_ptr<StaticModelAsset> AssetManager::LoadStaticModelAsset(string filePath)
{
    shared_ptr<StaticModelAsset> empty;
    return empty;
}

shared_ptr<RigidModelAsset> AssetManager::LoadRigidModelAsset(string filePath)
{
    shared_ptr<RigidModelAsset> empty;
    return empty;
}

shared_ptr<SkeletalModelAsset> AssetManager::LoadSkeletalModelAsset(string filePath)
{
    shared_ptr<SkeletalModelAsset> empty;
    return empty;
}